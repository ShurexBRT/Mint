# Optional. Startup requires no credentials. Values can also be set in your shell.
PORT=4310
DATABASE_PATH=./data/mint.sqlite
# Must remain 0; any other value prevents startup.
SPEND_LIMIT=0
# Threshold overrides are JSON and validated at startup.
# MINT_RULES_JSON={"minEvidence":5,"minDomains":2,"validateScore":45,"buildScore":75}
